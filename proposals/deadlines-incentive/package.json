{
  "name": "@ubiquity-os/command-deadline",
  "version": "1.0.0",
  "description": "Deadline tracking and incentive rewards for UbiquityOS",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@ubiquity-os/plugin-sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
