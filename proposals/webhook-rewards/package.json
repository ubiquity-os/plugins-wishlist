{
  "name": "webhook-rewards",
  "version": "0.1.0",
  "description": "Zero-config UbiquityOS plugin: reward contributors on PR merge / issue close based on timeline events and labels",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "@ubiquity-os/plugin-sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "keywords": ["ubiquity-os", "webhook", "rewards", "contributor", "bounty"],
  "license": "MIT"
}
