{
  "name": "@ubiquity-os/command-plan-plugin",
  "version": "1.0.0",
  "description": "Command plugin that breaks specs into child GitHub Issues via /plan command",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "knip",
    "test": "jest"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "@ubiquity-os/plugin-sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "knip": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
